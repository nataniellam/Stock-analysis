# Leave unset for local development (uses the Vite dev-server proxy to localhost:3001).
# In production (e.g. Vercel), set this to your deployed backend's URL, no trailing slash:
# VITE_API_URL=https://stockwise-backend.onrender.com
VITE_API_URL=
